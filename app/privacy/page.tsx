import type { Metadata } from "next";
import { getPublicSettings } from "@/lib/server/public-settings";
export const metadata: Metadata = { title: "Privacy policy" };
export default async function PrivacyPage() { const settings = await getPublicSettings(); if (settings.policiesApprovedAt && settings.privacyPolicy) return <main className="container policy-page"><h1 className="section-title">Privacy policy</h1>{settings.privacyPolicy.map((section) => <section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</section>)}</main>; return <main className="container policy-page"><span className="eyebrow">Owner review required</span><h1 className="section-title">Privacy policy</h1><div className="preview-block"><strong>This policy has not been approved for publication.</strong><span>The owner must confirm retention and deletion practices, the public contact email, and the final wording before online ordering is enabled.</span></div><h2>Information used for pickup orders</h2><p>The checkout is designed to collect only the customer name, email, phone number, optional order note, and payment references needed to fulfil and reconcile an order. Card details are entered directly on Stripe Checkout and are not collected by this application.</p><h2>Owner decisions still needed</h2><p>Retention duration, deletion request handling, legal contact details, and final privacy language remain configurable in the operator settings.</p></main>; }
