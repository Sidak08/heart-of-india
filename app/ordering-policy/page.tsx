import type { Metadata } from "next";
import { getPublicSettings } from "@/lib/server/public-settings";
export const metadata: Metadata = { title: "Ordering and refund policy" };
export default async function OrderingPolicyPage() { const settings = await getPublicSettings(); if (settings.policiesApprovedAt && settings.orderingPolicy) return <main className="container policy-page"><h1 className="section-title">Ordering & refund policy</h1>{settings.orderingPolicy.map((section) => <section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</section>)}</main>; return <main className="container policy-page"><span className="eyebrow">Owner review required</span><h1 className="section-title">Ordering & refund policy</h1><div className="preview-block"><strong>Online checkout remains disabled until this policy is approved.</strong><span>Pickup hours, cutoff, preparation estimate, cancellation terms, and refund handling still require owner confirmation.</span></div><h2>Pickup orders</h2><p>The ordering flow is configured for pickup at 89 Clarence St., Brampton. Reaching the payment confirmation page confirms payment only; it does not state that the kitchen has accepted the order or guarantee a preparation time.</p><h2>Contact the restaurant</h2><p>For questions about a pickup order, call <a href="tel:+19055005382">905-500-5382</a>.</p></main>; }
