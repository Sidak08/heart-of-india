"use client";
import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import { useCart } from "@/components/cart-provider";
import { formatCad } from "@/lib/menu";
import type { Quote } from "@/lib/server/quote";

type Errors = Partial<Record<"name" | "email" | "phone" | "notes", string>>;
function validate(form: FormData) {
  const errors: Errors = {}; const name = String(form.get("name") ?? "").trim(); const email = String(form.get("email") ?? "").trim(); const phone = String(form.get("phone") ?? "").trim(); const notes = String(form.get("notes") ?? "").trim();
  if (name.length < 2 || name.length > 80) errors.name = "Enter a name between 2 and 80 characters.";
  if (!/^\S+@\S+\.\S+$/.test(email) || email.length > 254) errors.email = "Enter a valid email address.";
  if (phone.length < 7 || phone.length > 30) errors.phone = "Enter a valid phone number.";
  if (notes.length > 500) errors.notes = "Notes must be 500 characters or fewer.";
  return { errors, values: { name, email, phone, notes } };
}

export function CheckoutClient() {
  const cart = useCart(); const [quote, setQuote] = useState<Quote | null>(null); const [quoteError, setQuoteError] = useState(""); const [loadingQuote, setLoadingQuote] = useState(false); const [submitting, setSubmitting] = useState(false); const [errors, setErrors] = useState<Errors>({}); const [submitError, setSubmitError] = useState(""); const formRef = useRef<HTMLFormElement>(null); const attemptId = useRef<string>("");
  useEffect(() => { attemptId.current ||= crypto.randomUUID(); }, []);
  const requestLines = useMemo(() => cart.lines.map(({ lineId, itemId, quantity, selections }) => ({ lineId, itemId, quantity, selections })), [cart.lines]);
  useEffect(() => { if (!cart.hydrated || !requestLines.length) return; const controller = new AbortController(); queueMicrotask(() => { if (controller.signal.aborted) return; setLoadingQuote(true); setQuoteError(""); fetch("/api/quote", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ lines: requestLines }), signal: controller.signal }).then(async (response) => { const data = await response.json(); if (!response.ok) throw new Error(data.error || "Quote failed."); setQuote(data); }).catch((error) => { if (error.name !== "AbortError") setQuoteError(error.message); }).finally(() => setLoadingQuote(false)); }); return () => controller.abort(); }, [cart.hydrated, requestLines]);
  if (!cart.hydrated) return <div className="container page-loading">Loading checkout…</div>;
  if (!cart.lines.length) return <main className="container empty-page"><h1 className="display">Your cart is empty</h1><p>Add something from the menu before checkout.</p><Link className="button-primary" href="/menu">View menu</Link></main>;
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSubmitError(""); const checked = validate(new FormData(event.currentTarget)); setErrors(checked.errors); const first = Object.keys(checked.errors)[0] as keyof Errors | undefined; if (first) { formRef.current?.querySelector<HTMLElement>(`[name="${first}"]`)?.focus(); return; } if (!quote?.orderable || !quote.signature || !attemptId.current) return; setSubmitting(true); try { const response = await fetch("/api/checkout", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ attemptId: attemptId.current, customer: checked.values, quote }) }); const data = await response.json(); if (response.status === 409 && data.quote) { setQuote(data.quote); attemptId.current = crypto.randomUUID(); throw new Error("The order changed. Review the updated total and try again."); } if (!response.ok || !data.url) throw new Error(data.error || "Secure payment could not be started."); window.location.assign(data.url); } catch (error) { setSubmitError(error instanceof Error ? error.message : "Secure payment could not be started."); setSubmitting(false); } }
  return <main className="container checkout-page"><div className="cart-page-head"><span className="eyebrow">Secure guest checkout</span><h1 className="section-title">Pickup details</h1><p>Your card details are entered on Stripe’s hosted checkout page.</p></div><div className="checkout-grid"><form ref={formRef} className="checkout-form" onSubmit={submit} noValidate><h2 className="display">Contact information</h2><div className="field"><label htmlFor="name">Name</label><input id="name" name="name" autoComplete="name" maxLength={80} aria-invalid={Boolean(errors.name)} aria-describedby={errors.name ? "name-error" : undefined} />{errors.name && <p id="name-error" className="field-error">{errors.name}</p>}</div><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" inputMode="email" autoComplete="email" maxLength={254} aria-invalid={Boolean(errors.email)} aria-describedby={errors.email ? "email-error" : undefined} />{errors.email && <p id="email-error" className="field-error">{errors.email}</p>}</div><div className="field"><label htmlFor="phone">Phone</label><input id="phone" name="phone" type="tel" inputMode="tel" autoComplete="tel" maxLength={30} aria-invalid={Boolean(errors.phone)} aria-describedby={errors.phone ? "phone-error" : undefined} />{errors.phone && <p id="phone-error" className="field-error">{errors.phone}</p>}</div><div className="field"><label htmlFor="notes">Order notes <span>(optional)</span></label><textarea id="notes" name="notes" rows={4} maxLength={500} aria-invalid={Boolean(errors.notes)} aria-describedby="notes-help" /><div className="field-help" id="notes-help">Up to 500 characters. The restaurant may not be able to accommodate every request.</div>{errors.notes && <p className="field-error">{errors.notes}</p>}</div></form><aside className="order-summary checkout-summary"><h2 className="display">Your order</h2>{loadingQuote && <p aria-live="polite">Checking current prices and availability…</p>}{quoteError && <div className="inline-error" role="alert"><strong>We could not confirm the order.</strong><span>{quoteError}</span></div>}{quote?.lines.map((line) => <div className="receipt-line" key={line.lineId}><div><strong>{line.quantity} × {line.name}</strong>{line.selections.map((selection) => <small key={selection.groupId}>{selection.groupLabel}: {selection.optionName}</small>)}</div><strong>{formatCad(line.lineTotalCents)}</strong></div>)}{quote && <><div className="summary-row"><span>Subtotal</span><strong>{formatCad(quote.subtotalCents)}</strong></div>{quote.taxBreakdown.map((tax) => <div className="summary-row" key={tax.label}><span>{tax.label}{tax.inclusive ? " (included)" : ""}</span><strong>{formatCad(tax.amountCents)}</strong></div>)}<div className="summary-row"><span>Tax</span><strong>{formatCad(quote.taxCents)}</strong></div>{quote.feeCents > 0 && <div className="summary-row"><span>Fee</span><strong>{formatCad(quote.feeCents)}</strong></div>}<div className="summary-row summary-total"><span>Total</span><strong>{formatCad(quote.totalCents)}</strong></div><div className="pickup-box"><strong>Pickup at</strong><span>{quote.pickupAddress.street}, {quote.pickupAddress.city}, {quote.pickupAddress.province} {quote.pickupAddress.postalCode}</span>{quote.pickupEstimateText && <span>{quote.pickupEstimateText}</span>}</div>{quote.blockers.length > 0 && <div className="preview-block" role="status"><strong>Online payment is not available yet.</strong>{quote.blockers.map((blocker) => <span key={blocker}>{blocker}</span>)}</div>}<button className="button-primary full" type="button" disabled={!quote.orderable || submitting} onClick={() => formRef.current?.requestSubmit()}>{submitting ? "Opening secure payment…" : "Continue to secure payment"}</button>{submitError && <p className="field-error" role="alert">{submitError}</p>}</>}</aside></div></main>;
}
