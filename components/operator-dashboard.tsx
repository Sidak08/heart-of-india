"use client";

import { AlertTriangle, Banknote, Check, Clock3, Mail, Phone, UserRound } from "lucide-react";
import { useRouter } from "next/navigation";
import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { playOrderTone } from "@/components/notification-settings";
import { formatTorontoDateTime, formatTorontoTime } from "@/lib/date-format";
import { formatCad } from "@/lib/menu";
import { allowedFulfillmentTransitions, fulfillmentLabels, type OrderDataIssue, type StoredOrder } from "@/lib/operations";
import type { NotificationJob } from "@/lib/server/sheets";

export type OperatorOrder = Omit<StoredOrder, "guestTokenHash" | "guestAccessExpiresAt" | "attemptId">;
type Filter = "active" | "completed" | "cancelled" | "all";

export function OperatorDashboard({ initialOrders, initialIssues, initialConflictCount, initialNotificationJobs, operatorEmail }: { initialOrders: OperatorOrder[]; initialIssues: OrderDataIssue[]; initialConflictCount: number; initialNotificationJobs: NotificationJob[]; operatorEmail: string }) {
  const router = useRouter(); const [orders, setOrders] = useState(initialOrders); const [issues, setIssues] = useState(initialIssues); const [conflictCount, setConflictCount] = useState(initialConflictCount); const [filter, setFilter] = useState<Filter>("active"); const [query, setQuery] = useState(""); const [notificationJobs, setNotificationJobs] = useState(initialNotificationJobs); const [notificationPending, setNotificationPending] = useState<string | null>(null); const [error, setError] = useState(""); const [updating, setUpdating] = useState<string | null>(null); const [announcement, setAnnouncement] = useState(""); const [refreshing, setRefreshing] = useState(false); const [lastCheckedAt, setLastCheckedAt] = useState<string | null>(null); const seen = useRef(new Set(initialOrders.map((order) => order.id))); const firstPoll = useRef(true); const refreshInFlight = useRef(false);
  const refresh = useCallback(async (announce = true) => {
    if (refreshInFlight.current) return; refreshInFlight.current = true; setRefreshing(true);
    try {
      const response = await fetch("/api/operator/orders", { cache: "no-store" }); if (response.status === 401) { router.replace("/operator/login"); return; }
      const data = await response.json(); if (!response.ok) throw new Error(data.error || "Orders could not be refreshed.");
      const incoming = data.orders as OperatorOrder[]; const newOrders = incoming.filter((order) => !seen.current.has(order.id)); incoming.forEach((order) => seen.current.add(order.id)); setOrders(incoming); setIssues(Array.isArray(data.issues) ? data.issues : []); setConflictCount(Number.isInteger(data.conflictCount) ? data.conflictCount : 0); setNotificationJobs(Array.isArray(data.notificationJobs) ? data.notificationJobs : []); setError("");
      setLastCheckedAt(data.checkedAt ?? new Date().toISOString());
      if (!firstPoll.current && newOrders.length && announce) { setAnnouncement(`${newOrders.length} new ${newOrders.length === 1 ? "order" : "orders"} received.`); if (localStorage.getItem("hoi-dashboard-sound") !== "off") playOrderTone(); }
      firstPoll.current = false;
    } catch (caught) {
      setError(caught instanceof Error ? caught.message : "Orders could not be refreshed.");
    } finally { refreshInFlight.current = false; setRefreshing(false); }
  }, [router]);
  useEffect(() => {
    const initial = window.setTimeout(() => void refresh(false), 0);
    const interval = window.setInterval(() => { if (document.visibilityState === "visible") void refresh(); }, 5000);
    const message = (event: MessageEvent) => { if (event.data?.type === "NEW_ORDER") void refresh(); };
    const resume = () => { if (document.visibilityState === "visible") void refresh(); };
    navigator.serviceWorker?.addEventListener("message", message);
    window.addEventListener("focus", resume); document.addEventListener("visibilitychange", resume);
    return () => { clearTimeout(initial); clearInterval(interval); navigator.serviceWorker?.removeEventListener("message", message); window.removeEventListener("focus", resume); document.removeEventListener("visibilitychange", resume); };
  }, [refresh]);
  useEffect(() => { if (!announcement) return; const timer = setTimeout(() => setAnnouncement(""), 5000); return () => clearTimeout(timer); }, [announcement]);
  const visible = useMemo(() => { const search = query.trim().toLowerCase(); return orders.filter((order) => {
    const matchesFilter = filter === "all" || (filter === "active" ? !["completed", "cancelled"].includes(order.fulfillmentStatus) : order.fulfillmentStatus === filter);
    const matchesSearch = !search || [order.orderNumber, order.customerName, order.customerEmail, order.customerPhone, order.createdAt].some((value) => value.toLowerCase().includes(search));
    return matchesFilter && matchesSearch;
  }); }, [orders, filter, query]);
  const counts = { active: orders.filter((order) => !["completed", "cancelled"].includes(order.fulfillmentStatus)).length, completed: orders.filter((order) => order.fulfillmentStatus === "completed").length, cancelled: orders.filter((order) => order.fulfillmentStatus === "cancelled").length, all: orders.length };
  async function update(order: OperatorOrder, patch: { fulfillmentStatus?: string; paymentStatus?: string }) {
    let reason: string | undefined;
    if (patch.fulfillmentStatus === "cancelled") { const supplied = window.prompt(`Reason for cancelling ${order.orderNumber}:`); if (!supplied?.trim()) return; reason = supplied.trim(); }
    if (patch.paymentStatus === "paid_at_store" && !window.confirm(`Confirm that payment was received for ${order.orderNumber}. This record cannot be reversed from the dashboard.`)) return;
    setUpdating(order.id); setError("");
    try {
      const response = await fetch(`/api/operator/orders/${order.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...patch, reason, expectedUpdatedAt: order.updatedAt }) }); const data = await response.json();
      if (!response.ok) throw new Error(data.error || "The order could not be updated.");
      const safe = data.order as OperatorOrder;
      setOrders((current) => current.map((entry) => entry.id === safe.id ? safe : entry)); setAnnouncement(`${safe.orderNumber} updated to ${fulfillmentLabels[safe.fulfillmentStatus]}.`);
    } catch (caught) { setError(caught instanceof Error ? caught.message : "The order could not be updated."); await refresh(false); }
    finally { setUpdating(null); }
  }
  async function retryNotification(orderId: string) {
    setNotificationPending(orderId); setError("");
    try { const response = await fetch(`/api/operator/orders/${orderId}/notification`, { method: "POST" }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Notification retry failed."); if (data.notification) setNotificationJobs((current) => [...current.filter((job) => job.orderId !== orderId), data.notification]); setAnnouncement(data.notification?.state === "sent" ? "A staff device accepted the notification." : "The notification attempt was recorded and will retry."); }
    catch (caught) { setError(caught instanceof Error ? caught.message : "Notification retry failed."); }
    finally { setNotificationPending(null); }
  }
  return <main className="container operator-page"><div className="operator-head"><div><span className="eyebrow">Live operations</span><h1 className="section-title">Pickup orders</h1><p>Signed in as {operatorEmail}</p></div></div><div className="operator-status-bar"><span><i className={refreshing ? "live-dot checking" : "live-dot"} aria-hidden="true" /> {refreshing ? "Checking for new orders…" : "Auto-refresh is on · every 5 seconds"}</span><div><small>{lastCheckedAt ? `Last checked ${formatTorontoTime(lastCheckedAt)}` : "Starting live updates…"}</small><strong>{counts.active} active</strong></div></div>{announcement && <div className="status-announcer operator-announcement" role="status">{announcement}</div>}{error && <div className="inline-error" role="alert"><strong>Dashboard update failed</strong><span>{error}</span><button className="text-action" type="button" onClick={() => refresh(false)}>Try again</button></div>}{(issues.length > 0 || conflictCount > 0) && <section className="operator-data-warning" role="alert"><AlertTriangle aria-hidden="true" /><div><h2>Some spreadsheet rows need attention</h2><p>Valid orders are still shown below. Fix the listed rows in the Orders sheet, then let the dashboard refresh.</p>{issues.length > 0 && <ul>{issues.slice(0, 20).map((issue) => <li key={issue.rowNumber}><strong>Row {issue.rowNumber}{issue.orderNumber ? ` · ${issue.orderNumber}` : ""}</strong><span>{issue.reasons.join("; ")}</span></li>)}</ul>}{issues.length > 20 && <p>{issues.length - 20} more invalid rows are not shown.</p>}{conflictCount > 0 && <p><strong>{conflictCount} concurrent change{conflictCount === 1 ? " was" : "s were"} safely ignored.</strong> The earliest committed version remains active.</p>}</div></section>}<nav className="operator-filters" aria-label="Order filters">{(["active", "completed", "cancelled", "all"] as Filter[]).map((value) => <button key={value} className={filter === value ? "active" : ""} type="button" onClick={() => setFilter(value)} aria-pressed={filter === value}>{value[0].toUpperCase() + value.slice(1)} <span>{counts[value]}</span></button>)}</nav><div className="operator-search field"><label htmlFor="order-search">Search recorded orders</label><input id="order-search" type="search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Order number, customer, email, phone, or date" /><small>Showing {visible.length} of {orders.length} recorded orders.</small></div>{!visible.length ? <div className="empty-operator"><Clock3 size={36} /><h2>No {filter === "all" ? "" : `${filter} `}orders</h2><p>New pickup orders will appear here automatically.</p></div> : <div className="operator-orders">{visible.map((order) => <article className={`operator-order status-${order.fulfillmentStatus}`} key={order.id}><header className="operator-order-head"><div><span className={`status-pill status-${order.fulfillmentStatus}`}>{fulfillmentLabels[order.fulfillmentStatus]}</span><h2>{order.orderNumber}</h2><time dateTime={order.createdAt}>{formatTorontoDateTime(order.createdAt)}</time></div><strong>{formatCad(order.totalCents)}</strong></header><div className="operator-customer"><span><UserRound size={17} /> {order.customerName}</span><a href={`mailto:${order.customerEmail}`}><Mail size={17} /> {order.customerEmail}</a><a href={`tel:${order.customerPhone}`}><Phone size={17} /> {order.customerPhone}</a></div><div className="operator-lines">{order.lines.map((line) => <span key={line.lineId}><b>{line.quantity} × {line.name}</b>{line.selections.map((selection) => ` · ${selection.optionName}`).join("")} <em>{formatCad(line.lineTotalCents)}</em></span>)}</div>{order.customerNotes && <p className="operator-notes"><strong>Notes:</strong> {order.customerNotes}</p>}{(() => { const job = notificationJobs.find((entry) => entry.orderId === order.id); return job ? <div className={`operator-notification-state state-${job.state}`}><span><strong>Staff notification:</strong> {job.state === "sent" ? "accepted by a subscribed device" : job.state === "sending" ? "sending" : job.state === "pending" ? "waiting to send" : "delivery failed"}{job.lastError ? ` · ${job.lastError}` : ""}</span>{job.state !== "sent" && <button type="button" className="text-action" disabled={notificationPending === order.id} onClick={() => retryNotification(order.id)}>{notificationPending === order.id ? "Retrying…" : "Retry now"}</button>}</div> : null; })()}{Boolean(order.statusHistory?.length) && <details className="operator-history"><summary>Change history ({order.statusHistory!.length})</summary><ol>{order.statusHistory!.map((entry, index) => <li key={`${entry.at}-${index}`}><time dateTime={entry.at}>{formatTorontoDateTime(entry.at)}</time> · {entry.actor} · {fulfillmentLabels[entry.fulfillmentFrom]} → {fulfillmentLabels[entry.fulfillmentTo]}{entry.paymentFrom !== entry.paymentTo ? ` · ${entry.paymentFrom} → ${entry.paymentTo}` : ""}{entry.reason ? ` · ${entry.reason}` : ""}</li>)}</ol></details>}<div className="operator-order-actions"><label><span>Kitchen status</span><select value={order.fulfillmentStatus} disabled={updating === order.id || allowedFulfillmentTransitions[order.fulfillmentStatus].length === 0} onChange={(event) => update(order, { fulfillmentStatus: event.target.value })}>{[order.fulfillmentStatus, ...allowedFulfillmentTransitions[order.fulfillmentStatus]].map((status) => <option value={status} key={status}>{fulfillmentLabels[status]}</option>)}</select></label><button className={order.paymentStatus === "paid_at_store" ? "payment-state paid" : "payment-state"} type="button" disabled={updating === order.id || order.paymentStatus === "paid_at_store"} onClick={() => update(order, { paymentStatus: "paid_at_store" })}>{order.paymentStatus === "paid_at_store" ? <Check size={18} /> : <Banknote size={18} />}{order.paymentStatus === "paid_at_store" ? "Paid at store" : "Mark paid at store"}</button>{updating === order.id && <span className="summary-note" role="status">Saving…</span>}</div></article>)}</div>}</main>;
}
