import { describe, expect, it } from "vitest";
import { createQuote, quoteRequestSchema, verifyQuoteSignature } from "@/lib/server/quote";

describe("server quote", () => {
  it("resolves trusted names and prices", async () => { const quote = await createQuote({ lines: [{ lineId: "11111111-1111-4111-8111-111111111111", itemId: "curries-butter-chicken", quantity: 2, selections: {} }] }); expect(quote.lines[0]).toMatchObject({ name: "Butter Chicken", unitPriceCents: 1599, lineTotalCents: 3198 }); expect(quote.totalCents).toBe(3198); expect(quote.orderable).toBe(false); const { signature, ...body } = quote; expect(verifyQuoteSignature(body, signature)).toBe(true); });
  it("rejects unknown items and bad option choices", async () => { await expect(createQuote({ lines: [{ lineId: "11111111-1111-4111-8111-111111111111", itemId: "made-up", quantity: 1, selections: {} }] })).rejects.toThrow(/no longer exist/i); await expect(createQuote({ lines: [{ lineId: "11111111-1111-4111-8111-111111111111", itemId: "curries-saag-chicken-goat-lamb", quantity: 1, selections: { protein: "tofu" } }] })).rejects.toThrow(/invalid choose protein/i); });
  it("rejects excessive quantities and request sizes", () => { expect(quoteRequestSchema.safeParse({ lines: [{ lineId: "11111111-1111-4111-8111-111111111111", itemId: "curries-butter-chicken", quantity: 21, selections: {} }] }).success).toBe(false); const lines = Array.from({ length: 3 }, (_, index) => ({ lineId: `11111111-1111-4111-8111-${String(index).padStart(12, "0")}`, itemId: "curries-butter-chicken", quantity: 20, selections: {} })); expect(quoteRequestSchema.safeParse({ lines }).success).toBe(false); });
  it("detects quote tampering", async () => { const quote = await createQuote({ lines: [{ lineId: "11111111-1111-4111-8111-111111111111", itemId: "curries-butter-chicken", quantity: 1, selections: {} }] }); const { signature, ...body } = quote; expect(verifyQuoteSignature({ ...body, totalCents: 1 }, signature)).toBe(false); });
});
